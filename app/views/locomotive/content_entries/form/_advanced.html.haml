= f.inputs :advanced do
  = f.input :_slug, required: false
  = f.input :_visible, as: :toggle

= f.actions back_url: content_entries_path(@content_type.slug), use_stored_location: params[:_location].present?
