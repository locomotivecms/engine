= f.inputs :information do
  = f.input :name
  = f.input :picture, as: :file
  = f.input :memberships, as: :array, template: :membership, new_item: { url: new_membership_path }, wrapper_html: { class: 'memberships' }

= f.actions do
  = locomotive_form_button @site
