= f.inputs name: :information do
  = f.input :name
  = f.input :picture, as: '::Locomotive::File'
  = f.input :memberships, as: 'Locomotive::Array', partial: :membership, new_item_url: new_membership_path, wrapper_html: { class: 'memberships' }

= f.actions class: 'text-right' do
  = semantic_form_button @site