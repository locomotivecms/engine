= f.input name,
  label:        custom_field_label(field),
  hint:         field.hint,
  as:           :number,
  wrapper_html: { class: [('highlighted' if highlighted), translatable_status(field)].compact.join(' ') }